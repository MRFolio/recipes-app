// import styles from './InfoContainer.module.scss';

interface InfoContainerProps {}

const InfoContainer = ({}: InfoContainerProps): JSX.Element => {
  return <div className="styles"></div>;
};

export default InfoContainer;
